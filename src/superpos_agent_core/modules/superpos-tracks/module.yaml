description: "Superpos tracks — list, get, create, patch tracks and link, unlink, or list issues; the bootstrap container for proposals (proposal → track → issues)."
env:
  - SUPERPOS_BASE_URL
  - SUPERPOS_HIVE_ID
  - SUPERPOS_AGENT_ID
  - SUPERPOS_API_TOKEN
  - SUPERPOS_REFRESH_TOKEN
