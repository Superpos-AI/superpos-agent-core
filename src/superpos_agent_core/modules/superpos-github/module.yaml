description: "GitHub via the Superpos credential proxy — call the GitHub REST API (PRs, issues, repos, Actions) without holding a token. Credentials are injected server-side."
env:
  - SUPERPOS_BASE_URL
  - SUPERPOS_HIVE_ID
  - SUPERPOS_AGENT_ID
  - SUPERPOS_API_TOKEN
  - SUPERPOS_REFRESH_TOKEN
